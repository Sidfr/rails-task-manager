<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<h1>LISTE DES TACHES</h1>
<% @tasks.each do |task| %>
<h2> <%= link_to "Delete", "/tasks/#{task.id}", method: :delete%>
<%= link_to "Edit", "/tasks/#{task.id}/edit", method: :get%>
<%= link_to "Show", "/tasks/#{task.id}", method: :get%>
<%= "#{task.id}: #{task.description} - #{task.author} (#{task.priority})" %> </h2>
<% end %>
